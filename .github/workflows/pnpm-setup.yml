name: pnpm-setup
on:
  workflow_dispatch:
jobs:
  setup-install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 7
          run_install: true
