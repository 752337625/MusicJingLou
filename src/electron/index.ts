const registerGlobElecreon = _app => {
  // window.ElectronAPI.getUserOpenFile((_event, file) => {
  //   ElMessage.info(file);
  // });
};
export default registerGlobElecreon;
export { registerGlobElecreon };
